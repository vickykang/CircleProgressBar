<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.vivam.circleprogressbar.MainActivity">

    <com.vivam.circleprogressbar.widget.CircleProgressBar
        android:id="@+id/circle_pb"
        android:layout_width="50dp"
        android:layout_height="50dp" />

    <EditText
        android:id="@+id/et_max"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/circle_pb"
        android:layout_marginTop="10dp"
        android:hint="input the max"
        android:inputType="numberDecimal" />

    <EditText
        android:id="@+id/et_progress"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/et_max"
        android:layout_marginTop="10dp"
        android:hint="input the progress"
        android:inputType="numberDecimal" />

    <Button
        android:id="@+id/btn_set"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/et_progress"
        android:layout_marginTop="10dp"
        android:text="Click" />

</RelativeLayout>
